# Fixtures

All companies here are **fictional**. The record *shapes* follow the real
Companies House Public Data API so parsing is tested against the real format,
but no company, number or address is real.

`api.company-information.service.gov.uk` is blocked by this build environment's
egress allowlist, so these connectors are fixture-tested only. **Live
verification is still required before relying on them.**
